# === FastAPI & Server ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-multipart==0.0.20        # multipart/form-data file uploads

# === Document ingest (focal-startup uploads) ===
pymupdf==1.25.1                 # PDF text extraction + page->image render for the vision fallback
python-docx==1.1.2              # .docx text extraction

# === Async Task Queue ===
celery[redis]==5.4.0
redis==5.2.1

# === LangGraph & LLM Providers ===
langgraph==1.2.2
langchain==1.3.2
langchain-openai==1.3.3
langchain-anthropic==1.4.8
langchain-google-genai==4.2.6
langchain-groq==1.1.3
langchain-core==1.4.8

# === Search / Research Tools ===
tavily-python==0.5.0
httpx==0.28.1

# === Auth (hosted multi-user sign-in via Clerk; imported lazily, only used when CLERK_ISSUER is set) ===
PyJWT[crypto]==2.9.0

# === Utilities ===
orjson==3.10.12
uuid7==0.1.0
